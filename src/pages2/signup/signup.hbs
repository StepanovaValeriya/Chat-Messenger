{{!< main/main}}
<div class="page__signup _page">
  <div class="auth">
    <h1 class="auth__title">Sign Up</h1>
    <form class="auth__form">
        {{#each profileInfo}}
          {{> "auth/auth" this}}
      {{/each}}
    </form>
    {{> "buttons/buttons" link="../chat/chat.hbs" text="Sign Up"}}
    <a class="auth__link" href="../login/login.hbs">Enter</a>
  </div>
</div>
